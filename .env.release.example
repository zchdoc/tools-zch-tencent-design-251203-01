# ============================
# 生产环境模板（推荐用于 Vercel）
# ============================

# 1) 前端路由基础路径（通常保持 /）
VITE_BASE_URL=/

# 2) API 前缀（需与 vercel.json rewrite 保持一致）
VITE_API_URL_PREFIX=/api

# 3) 请求代理开关（保留兼容，当前代码主要依赖 VITE_API_URL + PREFIX）
VITE_IS_REQUEST_PROXY=true

# 4) 推荐：同源 API（最稳妥）
#    前端请求 /api/**，由 Vercel rewrites 转发到后端
#    可避免 HTTPS 页面请求 HTTP 后端导致的混合内容问题
VITE_API_URL=

# 5) 可选：仅当后端也提供 HTTPS 时，才可启用直连
# VITE_API_URL=https://your-backend.example.com

# 6) 仅用于本地开发日志中的目标展示，不影响生产转发
#    如无需要可不设置
VITE_AUTH_API_TARGET=http://218.29.176.224:8079
VITE_API_TARGET=http://127.0.0.1:3000
