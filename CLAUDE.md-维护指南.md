# CLAUDE.md 维护指南

本文档说明如何维护项目中的 `CLAUDE.md` 文件，确保其内容始终与代码库保持同步。

---

## 一、什么时候需要更新 CLAUDE.md？

**不需要每次代码提交都更新**，仅在以下**架构级变化**发生时更新：

| 变化类型 | 示例 |
|---------|------|
| 构建/开发命令变更 | 新增 `npm run test:e2e`、修改 `build` 脚本 |
| 技术栈变更 | 引入 Vitest、替换 ESLint 配置、新增 Tailwind CSS |
| 核心架构调整 | 路由模式变更、权限系统重构、状态管理库替换 |
| 目录/文件约定变化 | 新增 `src/composables/`、修改组件命名规范 |
| 环境变量变化 | 新增 `VITE_XXX` 变量、修改代理配置 |
| 开发流程变化 | 新增 CI/CD 步骤、修改代码提交规范 |

**不需要更新的情况**：
- 新增普通页面或组件
- 修改业务逻辑
- 修复样式或文案
- 升级 patch/minor 版本的依赖

---

## 二、维护命令

### 1. 初始化 CLAUDE.md（仅首次使用）

```
/init
```

**用途**：分析代码库，从零创建 `CLAUDE.md` 文件。

**注意**：如果项目中已有 `CLAUDE.md`，此命令会询问是否覆盖或改进。

---

### 2. 审计并改进现有 CLAUDE.md（推荐）

```
/claude-md-improver
```

**用途**：
- 扫描仓库中所有 `CLAUDE.md` 文件
- 评估内容质量（完整性、准确性、时效性）
- 对比当前代码现状，识别过时或缺失的信息
- 自动生成改进建议并更新

**使用场景**：
- 发现 CLAUDE.md 中某些命令已失效
- 架构变化后需要同步文档
- 定期审计（建议每 1-2 个月执行一次）

---

### 3. 会话结束后自动更新

```
/revise-claude-md
```

**用途**：在当前会话结束前，将会话中发现的代码库新知识写回 `CLAUDE.md`。

**使用场景**：
- 一次开发会话中深入理解了某个模块的架构
- 修复问题时发现了文档中未提及的关键信息
- 想记录本次会话的"经验教训"

---

## 三、最佳实践

### 定期审计

建议每隔 **1-2 个月** 执行一次：

```
/claude-md-improver
```

检查 `CLAUDE.md` 是否仍然准确反映当前代码库状态。

### 重大变更后立即更新

当完成以下类型的工作后，应立即更新：

1. 重构了路由或权限系统
2. 新增了构建脚本或修改了开发流程
3. 引入了新的开发工具或依赖
4. 修改了环境变量或代理配置

### 保持简洁

`CLAUDE.md` 是**快速上手指南**，不是完整文档：

- ❌ 不要记录每个组件的用途
- ❌ 不要复制 README.md 的内容
- ✅ 只记录"需要读多个文件才能理解"的架构信息
- ✅ 只记录常用的开发命令

### 版本控制

`CLAUDE.md` 应该纳入 Git 版本控制：

```bash
git add CLAUDE.md
git commit -m "docs: 更新 CLAUDE.md，同步权限系统重构"
```

---

## 四、快速对照表

| 命令 | 用途 | 使用时机 |
|------|------|---------|
| `/init` | 创建 CLAUDE.md | 项目首次使用 Claude Code |
| `/claude-md-improver` | 审计并改进现有文件 | 定期维护或架构变更后 |
| `/revise-claude-md` | 会话结束后增量更新 | 单次会话中有新发现 |

---

## 五、本项目的维护记录

| 日期 | 操作 | 说明 |
|------|------|------|
| 2026-05-29 | `/init` 初始化 | 创建初始 CLAUDE.md |
